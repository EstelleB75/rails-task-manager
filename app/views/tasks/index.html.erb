<h1>All tasks</h1>

<%= link_to "Add a task", new_task_path %>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to(task.title, task_path(task)) %>
      <%= task.completed ? "✅" : "❌" %>
    </li>
  <% end %>
</ul>



